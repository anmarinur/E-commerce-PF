/* const { Product, Order, OrderDetail } = require("../db.js");

const chargeOrderDetails = async () => {
 try {
  let orderData = await Order.findAll(); //chequeo que haya ordenes de compra
  if(orderData){
   //let id_order = await Product.
  }
 } catch (error) {
  return error.message;
 }
}

module.exports = chargeOrderDetails; */
